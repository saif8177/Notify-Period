export interface AdmobAdsPlugin {
    echo(options: {
        value: string;
    }): Promise<{
        value: string;
    }>;
    loadNativeAd(options: {
        adId: string;
    }): Promise<{
        res: any;
    }>;
}
