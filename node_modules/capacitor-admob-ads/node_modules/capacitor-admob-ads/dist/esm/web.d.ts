import { WebPlugin } from '@capacitor/core';
import type { AdmobAdsPlugin } from './definitions';
export declare class AdmobAdsWeb extends WebPlugin implements AdmobAdsPlugin {
    loadNativeAd(options: {
        adId: string;
    }): Promise<{
        res: any;
    }>;
    echo(options: {
        value: string;
    }): Promise<{
        value: string;
    }>;
}
